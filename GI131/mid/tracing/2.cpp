#include <iostream>

using namespace std;
int main(){
    // تحويل الخوارزمية لكود
    ////////////////الكود هذا بيش نضمن ان الحل صح بس متدوروهش////////////
    int a = 3 , b = 5, c = 4 , x;
  
    x = a + c + c;
    
    a > b ? x = x + c : a = x + c;

    cout << "a = " << a << " b = " << b << " c = " <<  c << " x = " << x << endl;


    return 0;
}